name: remove-legacy-desktop

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Verify cleanup workflow registration
        run: echo "cleanup workflow registered"