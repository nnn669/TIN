TASK: 工具调用循环无轮次上限（最严重）
STEP: 已完成
DONE: 新增 lib/core/services/api/tool_loop_guard.dart，ToolLoopGuard 在共享 ToolCallHandler 上设闸：软上限 50 次（返回 tool_error 让模型用已有结果收尾，不抛异常）、硬上限 88 次请求（抛 ToolLoopBudgetExceeded 终止本轮）、同名同参连续 3 次判死循环。硬上限按 attemptCount 计（含被拒次数），避免模型无视拒绝时永远触不到硬上限。签名对 Map 键递归排序，防 provider 键序不稳导致重复检测失效。generation_controller.dart 的 buildToolCallHandler 包一层守卫，每次生成新建实例即每轮一个预算。成功结果按完整签名复用，并共享并发中的同一 Future；工具错误、MCP isError、异常和失败结果均清除缓存并保留重试能力。结果缓存现仅允许 search_web、Kelivo Fetch 的四个只读工具及其映射名，记忆、神经权能网关、本地工具和任意 MCP 工具不会复用副作用结果。新增 test/core/services/api/tool_loop_guard_test.dart 与 test/core/services/api/tool_call_result_cache_policy_test.dart 回归覆盖。选择守卫 ToolCallHandler 而非在 provider 的 while(true) 内插计数器：执行工具是 provider 取得下一轮 payload 的唯一途径，单点覆盖 claude_official / google_common / google_vertex / openai_common 及流式与非流式两条路径。
NEXT: 等待最终 Actions 验证