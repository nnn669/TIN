TASK: 工具调用循环无轮次上限（最严重）
STEP: 已完成
DONE: 新增 lib/core/services/api/tool_loop_guard.dart，ToolLoopGuard 在共享 ToolCallHandler 上设闸：软上限 16 次（返回 tool_error 让模型用已有结果收尾，不抛异常）、硬上限 20 次请求（抛 ToolLoopBudgetExceeded 终止本轮）、同名同参连续 3 次判死循环。硬上限按 attemptCount 计（含被拒次数），避免模型无视拒绝时永远触不到硬上限。签名对 Map 键递归排序，防 provider 键序不稳导致重复检测失效。generation_controller.dart 的 buildToolCallHandler 包一层守卫，每次生成新建实例即每轮一个预算。新增 test/core/services/api/tool_loop_guard_test.dart 共 16 个用例。选择守卫 ToolCallHandler 而非在四个 provider 的 while(true) 内插计数器：执行工具是 provider 取得下一轮 payload 的唯一途径，单点覆盖 claude_official / google_common / google_vertex / openai_common 及流式与非流式两条路径。版本号 bump 到 1.1.32+9026。
NEXT: 无，等待下一个任务